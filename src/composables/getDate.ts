const date = new Date()
const currentMonth = date.getMonth()
const currentYear = date.getFullYear()
const currentDay = date.getUTCDate()
const days: string[] = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]


 export{currentMonth,days,currentYear,currentDay}